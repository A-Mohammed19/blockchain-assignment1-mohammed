# === Node.js dependencies ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
pnpm-lock.yaml
# === Logs & temporary files ===
*.log
*.out
*.err
*.pid
*.seed
*.pid.lock
# === System / editor files ===
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
# === Environment & secrets ===
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# === Compiled output / builds ===
dist/
build/
.cache/
.tmp/
# === OS-generated ===
ehthumbs.db
Icon?
Desktop.ini
# === Fabric/Blockchain artifacts (future assignments) ===
fabric-config/
crypto-config/
channel-artifacts/
wallet/
Apply it (if you already committed files before adding .gitignore):
git rm -r --cached .
git add .
git commit -m "Add .gitignore and clean repo"
git push.vscode/
.idea/
*.swp
*.swo
# === Environment & secrets ===
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# === Compiled output / builds ===
dist/
build/
.cache/
.tmp/
# === OS-generated ===
ehthumbs.db
Icon?
Desktop.ini
# === Fabric/Blockchain artifacts (future assignments) ===
fabric-config/
crypto-config/
channel-artifacts/
wallet/
